{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "Install and Run Backend & Frontend",
			"command": "cd Backend && npm install && npx hardhat node & cd ../Frontend && npm install && npm run dev",
			"isBackground": false,
			"group": "build"
		}
	]
}